{
  "name": "chess-opening-practice",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "release:check": "npm run build && vitest run test/release-readiness.test.ts",
    "test:emulators": "firebase emulators:exec --only auth,firestore \"vitest run test\""
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-tools": "^14.10.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
